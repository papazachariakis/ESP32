#pragma once

// Copy to wifi_secrets.h and fill in your WiFi passwords.
// wifi_secrets.h is gitignored — never commit real passwords.

#define WIFI_SEED_COUNT 2

struct WifiSeed {
  const char* ssid;
  const char* pass;
};

static const WifiSeed WIFI_SEEDS[] = {
  {"mikrotik", "YOUR_MIKROTIK_PASSWORD"},
  {"kalithea", "YOUR_KALITHEA_PASSWORD"},
};
